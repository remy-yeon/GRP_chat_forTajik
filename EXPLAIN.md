# Tajikistan RAG API – 대사관·병원 다국어 설계 설명

이 문서는 **초기 다국어 RAG 설계가 실패한 이유**와  
**현재 대사관·병원 정보에 적용된 안정적인 구조가 어떻게 동작하는지**를 설명합니다.

---

## 1. 초기 설계가 실패한 이유

### 1.1 초기 구상 방식

초기에는 다음과 같은 흐름을 목표로 설계했습니다.

1. RAG를 통해 문서에서 사실 정보(영어 기준)를 검색
2. 해당 정보를 기반으로 영어 문장 생성
3. 대사관명, 주소, 전화번호 등 고유명사를 러시아어로 매핑
4. 영어와 러시아어가 섞인 문장을 LLM에 입력하여 러시아어 문장으로 변환

그러나 이 방식은 **정확성이 중요한 도메인**에서는 신뢰할 수 없는 결과를 만들었습니다.

---

### 1.2 영어·러시아어 혼합 입력의 문제

영어와 러시아어가 섞인 텍스트를 LLM에 입력하면,  
LLM은 이를 **하나의 번역 대상 텍스트**로 인식합니다.

그 결과 이미 러시아어로 매핑한:
- 대사관 이름
- 주소
- 전화번호

까지도 다시 번역하거나, 순서를 바꾸거나, 표현을 변형하는 문제가 발생했습니다.

이는 프롬프트 설정의 문제가 아니라  
**LLM의 언어 처리 특성상 피하기 어려운 한계**입니다.

---

### 1.3 “고유명사는 번역하지 말 것” 규칙의 한계

아무리 프롬프트에 다음과 같은 규칙을 명시해도:

- 고유명사는 번역하지 말 것
- 주소와 전화번호를 수정하지 말 것

LLM은 이를 **100% 준수하지 않습니다**.

특히:
- 소형 모델(Gemma2 2B)
- 번역과 문장 자연화를 동시에 요구하는 상황

에서는 모델이  
**규칙 준수보다 문장 자연스러움을 우선**하는 경향이 강했습니다.

---

### 1.4 주소·전화번호 데이터의 특수성

주소와 전화번호는 **조금만 변경되어도 틀린 정보**가 됩니다.

LLM 번역 과정에서 다음과 같은 변화가 확률적으로 발생했습니다.

- 주소 순서 변경
- 약어 ↔ 풀네임 변환
- 기호 또는 공백 변경

이러한 문제는 프롬프트만으로 완전히 방지할 수 없었습니다.

---

### 1.5 결과적 문제점

이로 인해 결과는 “가끔 틀리는 수준”이 아니라  
**신뢰할 수 없는 시스템**이 되었습니다.

대사관·병원 정보처럼 **정확성이 최우선인 도메인**에서는  
이 구조를 유지할 수 없다고 판단했습니다.

---

## 2. 현재 구현된 구조

### 한 줄 요약

> **RAG는 사실을 찾는 역할만 수행하고,  
> 언어는 초기에 확정하여 분기 처리하며,  
> LLM은 번역이 아닌 ‘문장화’만 담당한다.**

---

## 3. 전체 처리 흐름 (대사관·병원 공통)

### 3.1 RAG: 검색 + 사실 추출

- Chroma (Dense Search)
- BM25 (Sparse Search)
- RRF (결과 병합)

목적:
- 대사관: 정확한 레코드 1개 선택
- 병원: 도시별 병원 레코드 여러 개 선택

이 단계는 **언어와 완전히 무관**합니다.

---

### 3.2 언어 결정

- `is_russian_with_history(question, history)`로 언어를 초기에 확정
- 결과:
  - English 모드
  - Russian 모드

이후 단계에서는 **언어가 절대 변경되지 않습니다**.

---

## 4. 영어 모드 파이프라인

### 4.1 데이터 처리 방식

- 문서에서 추출한 사실 정보를 그대로 사용
  - EmbassyName / Address / Phone
  - HospitalName / Address / Phone
- 번역 없음
- 매핑 없음

---

### 4.2 LLM 역할

- 프롬프트를 통해 값 수정 금지 및 형식 제한
- LLM은:
  - 추출된 레코드를 **자연스러운 영어 문장으로 변환**하는 역할만 수행

> LLM = 영어 문장 생성기

---

## 5. 러시아어 모드 파이프라인 (핵심 구조)

### 5.1 문서 기반 fact의 역할

RAG는 러시아어 모드에서도 다음 판별에만 사용됩니다.

- 해당 문서가 어떤 국가의 대사관인지
- 어떤 병원 레코드에 해당하는지

즉, **트리거 역할**만 수행합니다.

---

### 5.2 하드맵 기반 값 치환

실제 출력에 사용되는 값은 하드맵에서 가져옵니다.

- 대사관: `EMBASSY_RU`
- 병원: `HOSPITAL_RU_MAP`

특징:
- 문서 내용 번역 없음
- 영어 값을 러시아어로 변환하지 않음
- 이미 검증된 러시아어 문자열을 그대로 사용

---

### 5.3 LLM 역할

- 이미 러시아어로 확정된 값들을 입력으로 받아
- 2~3문장의 자연스러운 러시아어 설명으로 조립

> LLM = 러시아어 문장 조립기

---

## 6. 한 문장 요약

> **현재 구조는  
> “RAG로 사실을 선택하고,  
> 언어별로 안전한 값을 확정한 뒤,  
> LLM은 문장만 만들어준다”는 설계다.**
