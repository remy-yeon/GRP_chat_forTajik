📋 PRD: Ollama RAG Chat Frontend
1. 개요
항목내용프로젝트명Ollama RAG Chat목적로컬 LLM 기반 문서 Q&A 데모타겟개발자/학생 (과제/데모용)플랫폼반응형 웹 (데스크톱 + 모바일)

2. 핵심 기능
기능우선순위설명채팅 UIP0질문/답변 대화형 표시, 세션 동안 히스토리 유지파일 업로드P0PDF/TXT 업로드 (입력창 📎 버튼)문서 관리P1사이드바에서 업로드된 문서 목록 확인/삭제로딩 상태P0답변 생성 중 타이핑 애니메이션출처 표시P1답변 하단에 참조 문서/페이지 표시반응형P0모바일에서 사이드바 → 햄버거 메뉴

3. UI 구조
데스크톱 (≥768px)                    모바일 (<768px)
┌──────────┬────────────────────┐    ┌────────────────────┐
│ 사이드바  │                    │    │ ☰  Ollama RAG Chat │
│          │                    │    ├────────────────────┤
│ 📁 문서   │    채팅 영역        │    │                    │
│ ────────│                    │    │    채팅 영역        │
│ doc1.pdf│  👤 질문 메시지      │    │                    │
│ doc2.txt│  🤖 답변 메시지      │    │  👤 질문           │
│          │                    │    │  🤖 답변           │
│ [전체삭제]│                    │    │                    │
├──────────┼────────────────────┤    ├────────────────────┤
│          │ 📎 │ 메시지 입력.. │➤│    │ 📎 │ 메시지 입력.. │➤│
└──────────┴────────────────────┘    └────────────────────┘

4. 디자인 스펙
요소값테마다크 모드 (ChatGPT 스타일)배경#212121 (메인), #171717 (사이드바)텍스트#ECECEC (기본), #8E8E8E (보조)액센트#10A37F (전송 버튼, 강조)폰트system-ui, 본문 15px모서리12px (메시지 버블), 24px (입력창)

5. API 연동
액션엔드포인트트리거파일 업로드POST /upload_file📎 클릭 → 파일 선택질문 전송POST /ask전송 버튼 or Enter문서 목록GET /list_documents페이지 로드, 업로드 완료 시문서 삭제DELETE /delete_document?doc_id=삭제 버튼 클릭전체 삭제DELETE /clear_all전체 삭제 버튼

6. 상태 관리 (프론트 세션)
javascriptstate = {
  messages: [
    { role: 'user', content: '질문...' },
    { role: 'assistant', content: '답변...', sources: [...] }
  ],
  documents: [
    { doc_id: 'xxx', source: 'file.pdf' }
  ],
  isLoading: false,
  sidebarOpen: true  // 모바일용
}

7. 구현 범위
단일 HTML 파일로 구현 (Vanilla JS + CSS):

외부 라이브러리 없음
<style> 태그에 CSS
<script> 태그에 JS
바로 브라우저에서 열어서 테스트 가능